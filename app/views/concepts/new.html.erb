<% @pagetitle = "Add a new concept" %>

<h1>Add a new concept</h1>

<% form_for(@concept) do |f| %>
<%= f.error_messages %>

<%= render(:partial => 'form', :object => f) %>

<p>
<%= f.submit "Create" %>
</p>
<% end %>

<%= link_to 'Back', concepts_path %>
