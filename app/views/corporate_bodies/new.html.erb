<% @pagetitle = "Add a new corporate body" %>

<h1>Add a new corporate body</h1>

<% form_for(@corporate_body) do |f| %>
<%= f.error_messages %>

<%= render(:partial => 'form', :object => f) %>

<p>
<%= f.submit "Create" %>
</p>
<% end %>

<%= link_to 'Back', corporate_bodies_path %>
