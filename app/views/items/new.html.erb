<% @pagetitle = "Add a new item" %>

<h1>Add a new item</h1>

<p>

Work:
<%= Expression.find(Manifestation.find(params[:manifestation_id]).expression_id).work.anchor_text %>
<br />
Expression:
<%= Expression.find(Manifestation.find(params[:manifestation_id]).expression_id).anchor_text %>
<br />
Manifestation:  
<%= Manifestation.find(params[:manifestation_id]).anchor_text %>

</p>

<% form_for(@item) do |f| %>
<%= f.error_messages %>

<p>

Owner:

<%= render(:partial => 'layouts/group2_entity_pick') %>

</p>

<%= render(:partial => 'form', :object => f) %>

<%= hidden_field (:item, :manifestation_id, :value => params[:manifestation_id]) %>

<p>
<%= f.submit "Create" %>
</p>
<% end %>

<%= link_to 'Back', items_path %>
