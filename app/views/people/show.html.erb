<% @pagetitle = "Person: #{@person.name}" %>

<h1><%= @person.name %></h1>

<%= link_to 'Edit', edit_person_path(@person) %>

<%= render(:partial => 'attributes', :object => @person) %>

<h2>Works created</h2>

<ul>

<%- @person.creations.each do |c| %>

<li> <%= link_to Work.find(c.work_id).title,
  :controller => "works",
  :action     => "show",
  :id         => c.work_id %> 
</li>

<%- end %>

</ul>

<h2>Expressions realized</h2>

<h2>Manifestations produced</h2>

<h2>Items owned</h2>



