<% @pagetitle = "Person: #{@person.name}" %>

<h1><%= @person.name %></h1>

<%= link_to 'Edit', edit_person_path(@person) %>

<%= render(:partial => 'attributes', :object => @person) %>

<p>

Creations:

</p>

<ul>

<%- @person.creators.each do |c| %>

<li> <%= c.relation %> <%= link_to Work.find(c.work_id).title,
  :controller => "works",
  :action     => "show",
  :id         => c.work_id %> 
</li>

<%- end %>

</ul>

