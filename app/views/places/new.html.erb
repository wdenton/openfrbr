<% @pagetitle = "Add a new place" %>

<h1>Add a new place</h1>

<% form_for(@place) do |f| %>
<%= f.error_messages %>

<%= render(:partial => 'form', :object => f) %>

<p>
<%= f.submit "Create" %>
</p>
<% end %>

<%= link_to 'Back', places_path %>
