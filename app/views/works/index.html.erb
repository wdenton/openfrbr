<% @pagetitle = "Works" %>

<h1>Works</h1>

<% for work in @works %>
<h2> <%= link_to work.title, work %> (<%= work.date %>)</h2>

<p>

Created by:

</p>

<%= render(:partial => 'layouts/group2entitylist', 
  :object => work.creators) %>

<%= render(:partial => 'attributes', :object => work) %>

<% end %>

<%= link_to 'New work', new_work_path %>
